MODULE LambdaExpressionsTC01;

TYPE
   ld = LAMBDA -> (INTEGER, INTEGER): INTEGER
VAR
    a: INTEGER;
    b: INTEGER;
    x : ld;

BEGIN
    x := (a: INTEGER, b: INTEGER) => a + b
END

END LambdaExpressionsTC01.
